name: weather_info
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.4.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  location: ^6.0.2
  http:

dev_dependencies:
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
